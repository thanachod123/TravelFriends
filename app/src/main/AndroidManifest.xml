<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finalproject.it.travelfriend">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".LoginUserActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <!-- FACEBOOK -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterGuideActivity"
            android:label="สมัครสมาชิกเป็นผู้นำเที่ยว"
            android:windowSoftInputMode="stateHidden|adjustPan"/>
        <activity android:name=".HomeUserActivity" />
        <activity android:name=".HomeGuideActivity" />
        <activity android:name=".LoginGuideActivity"
            android:label="เข้าสู่ระบบผู้นำเที่ยว"
            android:windowSoftInputMode="stateHidden|adjustPan"/>
        <activity android:name=".MainGuideActivity"/>
        <activity android:name=".Guide.EditProfileGuide"
            android:label="แก้ไขโปรไฟล์"
            android:windowSoftInputMode="stateHidden|adjustPan"/>
        <activity android:name=".Guide.ChangePasswordGuide"
            android:label="เปลี่ยนรหัสผ่าน"
            android:windowSoftInputMode="stateHidden|adjustPan"/>
        <activity android:name=".Guide.CreatePackage.CreatePackageGuide"
            android:label="แพ็คเกจนำเที่ยว"
            android:windowSoftInputMode="stateHidden|adjustPan"/>
    </application>

</manifest>